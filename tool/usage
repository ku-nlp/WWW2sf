USAGE:
  WWW2sf/tool/www2sf.sh [-j|-k|-s] [-u|-U] [-z] [-c somewhere/cns.cdb] [-S] [-T] [-e|-E] [-x] [-a] [-d SynGraphPath] hdir xdir

OPTION:
  -j    ... jumanの解析結果を埋め込む.

  -k    ... knpの解析結果を埋め込む.

  -s    ... SynGraphの解析結果を埋め込む.

  -u    ... utf8に変換したHTML文書を出力する.
            utf8化されたHTML文書はXXX.YYY.utf8.htmlという形式でhdir以下に出力される.
            (XXXは文書ID, YYYはプロセス番号)

  -U    ... hdirで指定したHTML文書がすでにutf8化されている場合.

  -z    ... hdirで指定したHTML文書がgzipされている場合.

  -c    ... アンカーテキストの連続を複合名詞単位で区切る.

  -S    ... 標準フォーマットに変換するHTML文書のファイルサイズの上限を指定.
            (デフォルトでは5MB以上のファイルは変換されない)

  -T    ... 領域判定を行う.

  -e    ... 英語モード.

  -E    ... 英語モード (構文解析は行わず、文抽出のみを行う).

  -x    ... 解析結果をXMLとして埋め込む.

  -a    ... KNPにおいて省略解析を行う.

  -d    ... SynGraphのパスを指定する.

  hdir  ... HTML文書が収められたディレクトリ.
            (utf8化されていないもの, gzipされていないもの)

  xdir  ... 標準フォーマットが出力されるディレクトリ.
